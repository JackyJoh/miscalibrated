# .env.example — Copy this to .env and fill in your values.
# .env is gitignored — never commit real secrets.
#
# Usage: cp .env.example .env

# ─── PostgreSQL ─────────────────────────────────────────────────
POSTGRES_USER=miscalibrated
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=miscalibrated
DATABASE_URL=postgresql+asyncpg://miscalibrated:devpassword@localhost:5432/miscalibrated

# ─── Kafka ──────────────────────────────────────────────────────
# Use localhost:29092 when running producers/consumers from the host machine.
# Use kafka:9092 when running from inside a Docker container.
KAFKA_BOOTSTRAP_SERVERS=localhost:29092

# ─── Auth0 ──────────────────────────────────────────────────────
# Get these from your Auth0 Application dashboard.
# DOMAIN:    your-tenant.us.auth0.com  (no https://)
# AUDIENCE:  the API Identifier you set when creating an Auth0 API
# ALGORITHMS: RS256 is the default for Auth0 JWTs — don't change this
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_AUDIENCE=https://api.miscalibrated.com
AUTH0_ALGORITHMS=RS256

# ─── Kalshi API ─────────────────────────────────────────────────
KALSHI_API_KEY=your-kalshi-api-key
KALSHI_BASE_URL=https://trading-api.kalshi.com/trade-api/v2

# ─── Polymarket ─────────────────────────────────────────────────
POLYMARKET_CLOB_URL=https://clob.polymarket.com

# ─── NewsAPI ────────────────────────────────────────────────────
NEWS_API_KEY=your-newsapi-key

# ─── SendGrid (email alerts) ────────────────────────────────────
SENDGRID_API_KEY=your-sendgrid-api-key
ALERT_FROM_EMAIL=alerts@miscalibrated.com

# ─── LLM Service ────────────────────────────────────────────────
# Set LLM_PROVIDER to "anthropic" or "openai" — the LLMService class
# reads this and routes calls accordingly (swappable, no code changes needed).
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=your-anthropic-api-key
OPENAI_API_KEY=your-openai-api-key

# ─── App ────────────────────────────────────────────────────────
ENVIRONMENT=development
LOG_LEVEL=DEBUG
CORS_ORIGINS=http://localhost:5173
