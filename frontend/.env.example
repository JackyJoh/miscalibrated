# frontend/.env.example — Vite exposes VITE_* vars to the browser bundle.
# Copy to .env.local and fill in your Auth0 values.
# Never commit .env.local — it's gitignored by Vite by default.

# From your Auth0 Application dashboard (Single Page Application type)
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your-spa-client-id

# The API Identifier you created in Auth0 → APIs
VITE_AUTH0_AUDIENCE=https://api.miscalibrated.com
